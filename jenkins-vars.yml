---

# jenkins variables
project_name: docker-doc-builder
external_type: os
release_type: stable
release_tag: latest
ls_branch: master
repo_vars:
  - BUILD_VERSION_ARG='BUILDER_VERSION'
  - LS_USER='linuxserver'
  - LS_REPO='docker-doc-builder'
  - CONTAINER_NAME='doc-builder'
  - DOCKERHUB_IMAGE='linuxserver/doc-builder'
  - DEV_DOCKERHUB_IMAGE='lsiodev/doc-builder'
  - PR_DOCKERHUB_IMAGE='lspipepr/doc-builder'
  - DIST_IMAGE='alpine'
  - MULTIARCH='true'
  - CI='true'
  - CI_WEB='false'
  - CI_PORT='8000'
  - CI_SSL='false'
  - CI_DELAY='60'
  - CI_DOCKERENV='CI_RUN=True'
  - CI_AUTH='user:password'
  - CI_WEBPATH=''
